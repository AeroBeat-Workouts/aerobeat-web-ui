// @ts-check

/**
 * Representative button states for scene validation.
 *
 * @type {Readonly<{
 *   default: { label: string },
 *   calibration: { label: string }
 * }>}
 */
export const aeroButtonDebugStates = Object.freeze({
  default: { label: "Continue" },
  calibration: { label: "Begin calibration" }
});
